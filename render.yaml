services:
  - type: web
    name: ceo-finder-clean
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 180 --log-level info
    envVars:
      - key: FLASK_SECRET
        value: change_me
